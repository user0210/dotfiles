[Unit]
Description=SSH tunnel to for the Minecraft port
After=network.target

[Service]
Type=simple
Restart=always
RestartSec=10
ExecStart=/usr/bin/ssh -o ServerAliveInterval=180 -o ServerAliveCountMax=2 -nNT -R 25565:localhost:25565 root@217.160.214.90

[Install]
WantedBy=default.target
